<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dotwee.openkwsolver"
    android:versionCode="2"
    android:versionName="beta.2">

    // .INTERNET is needed to pull CaptchaID, the actual Captcha picture and send answers
    <uses-permission android:name="android.permission.INTERNET" />
    // .VIBRATE is needed for vibrating the devices, as soon as a new captcha arrives
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".TextActivity"
            android:label="@string/title_activity_text"></activity>
        <activity
            android:name=".ConfirmActivity"
            android:label="@string/title_activity_confirm"></activity>
        <activity
            android:name=".ClickActivity"
            android:label="@string/title_activity_click"></activity>
    </application>

</manifest>
